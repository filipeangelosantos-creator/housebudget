# Your domain (DNS must already point at this server before starting)
DOMAIN=budget.example.com

# Where the database, uploaded statements and signing key are kept.
# Deliberately outside the code checkout, so deleting or re-cloning the
# repository never touches your data. Back this directory up.
HB_DATA_PATH=/srv/housebudget-data
